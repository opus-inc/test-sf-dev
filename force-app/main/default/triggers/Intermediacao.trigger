trigger Intermediacao on Intermediacao__c (after insert, after update, before delete) {
    //IntermediacaoTriggerHandler handler = new IntermediacaoTriggerHandler(Trigger.isExecuting, Trigger.size);
    //
    //if( Trigger.isInsert ) {
    //    if(Trigger.isAfter) {
    //       handler.OnAfterInsert(trigger.New);
    //    }
    //} else if ( Trigger.isUpdate ) {
    //    if(Trigger.isAfter) {
    //        handler.OnAfterUpdate(trigger.New ,trigger.Old,Trigger.NewMap,Trigger.OldMap);
    //    }
    //} else if ( Trigger.isDelete ) {
    //    if(Trigger.isBefore) {
    //        handler.OnBeforeDelete(Trigger.old);
    //    }
    //}
}